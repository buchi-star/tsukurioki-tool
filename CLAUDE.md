# CLAUDE.md — 作り置き献立メーカー

このファイルは、Claude Code / Cursor がこのプロジェクトを開いたときに最初に読む「取扱説明書」です。
ここに目的・制約・ルールを書いておくことで、毎回ゼロから説明し直す必要がなくなります。

-----

## プロジェクト概要

- **何を作るか**：家にある食材から、その週の作り置き献立を提案するツール
- **誰のためか**：作り手本人（40代・単身世帯・平日リモート＋週末出社）
- **第一弾の範囲**：「家にある食材」＋「制約条件」を入力 → 作り置きおかずを3〜5品提案する。これ1機能だけ
- **第二弾以降に回すもの**：栄養バランスの可視化、買い物リスト生成、献立カレンダー、在庫管理、コンテンツ管理。第一弾が完成・公開できるまで着手しない

-----

## 制約条件（料理の前提）

提案する料理は、必ず以下の条件をすべて満たすこと。

### 調理環境

- 電子レンジは使えない
- 加熱は「魚焼きグリル」「電気圧力鍋」「コンロ」のいずれかのみ
- 作り置きとして数日もつことを前提にする

### 食事方針

- 1日1食、夕食中心の生活
- 魚をメインにする。肉は控えめ
- 高たんぱく
- 抗炎症・抗酸化を重視する
- 野菜をたっぷり使う。炭水化物はできるだけ野菜で代用する
- 揚げ物は作らない
- 油はオリーブオイル中心。バターは控えめにする
- 加工品はできるだけ避け、自然素材の調味料を優先する
- 節約を意識する（基本は家にある食材だけで完結させる）

-----

## 技術ルール

- 食材＋制約から献立を生成する処理は、Claude API を使う
- AI からの出力は、決められた JSON 形式で返させる（前後に説明文を付けさせない）
- JSON が途中で壊れても画面が止まらないよう、テキスト表示にフォールバックする
- コード内のコメントは日本語で書く
- コードは「初学者本人が読んで大筋を追える」素朴な書き方にする。技巧的な書き方は避ける
- 機能を詰め込まない。1機能ずつ、動く状態まで完成させてから次に進む
- 大きな変更を加える前に、影響範囲を先に説明する
- `.env` ファイルの中身は絶対に表示しない。確認を求められた場合も「設定済み／未設定」の事実だけを答える

-----

## 進め方のルール（重要）

- いきなり実装させない。まず「何を・なぜ・どの範囲で」を整理してから依頼する
- 実装の前に、設計（やること・構成・手順）を先に出させて確認する
- 「もっとシンプルな方法はないか？」を一度問うてから実装に進む
- 詰まったときは、原因を切り分けてから直す。「とりあえず直して」では投げない

-----

## ディレクトリ構成

（プロジェクトを作り始めたら、ここに実際の構成を追記する。
既存コードがある状態なら Claude Code で `/init` を使うと自動でたたき台が作れる）

-----

## よく使うコマンド

（開発環境を整えたら、ここに起動・テスト・確認のコマンドを追記する）

-----

## 過去のミスから学んだルール

ここは「育てる」欄です。詰まって直すたびに、再発防止ルールを1行ずつ足していくこと。
長く書くことが目的ではなく、「実際に問題になったことだけ」を書きます。

- ブラウザのサンドボックス環境では、blob URL 経由の画像読み込みが弾かれることがある。画像は FileReader で直接 base64 化する
- iPhone の写真は HEIC 形式のことがあり、ブラウザが直接開けない。変換処理を用意しておく
- スマホの画像はサイズが大きく、そのままだと API に通らないことがある。送信前にサイズを確認する
- 機能を最初から詰め込むと完成が遠のく。第一弾は1機能に絞る
- 実装を先に走らせると、方向違いのまま手戻りが出る。設計を先に固める
- Claude APIはレスポンスを ```json ... ``` のmarkdownコードブロックで返すことがある。JSON.parseの前にコードブロックを除去する処理を必ず入れる

（以下、詰まるたびに追記）

-----

## 次回の開始点

- .env にAPIキーを記入し、`node test.js` を実行してJSONが返るか確認する